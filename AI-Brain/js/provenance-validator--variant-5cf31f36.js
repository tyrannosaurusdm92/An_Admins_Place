(function(root){'use strict';const A=root.AIBrain=root.AIBrain||{};A.validateProvenance=function(item){const p=item?.provenance;const warnings=[];if(!p)warnings.push('No provenance metadata.');else{if(!p.source)warnings.push('Missing provenance.source.');if(!p.originalPath&&!p.note&&!p.preservationRule)warnings.push('No source path or provenance note.')}return{ok:!warnings.length,warnings};};})(typeof globalThis!=='undefined'?globalThis:this);
