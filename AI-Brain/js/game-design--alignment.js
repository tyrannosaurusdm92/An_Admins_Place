/* Genericized for AI-Brain capability use. Provenance group: active-session-runtime-c. */
(function(){
  'use strict';
  const U=window.PZUtils,A=window.ActiveWorkspace_DATA.alignment;
  const Quiz={answers:{},questionIndex:0,mode:'quick',creation:{background:[],covenant:[],classFocus:[]},trackers:{resonance:0,divineClaim:0,resolve:3,convictionStrain:0}};
  const axes=A.axes||[],axisById=Object.fromEntries(axes.map(x=>[x.id,x]));
  Quiz.questions=()=>Quiz.mode==='deep'?A.deepQuestions:A.quickQuestions;
  Quiz.reset=(mode=Quiz.mode)=>{Quiz.answers={};Quiz.questionIndex=0;Quiz.mode=mode;Quiz.creation={background:[],covenant:[],classFocus:[]};return Quiz};
  Quiz.answer=(questionIndex,optionIndex)=>{Quiz.answers[questionIndex]=optionIndex;return Quiz.calculate()};
  Quiz.setMode=mode=>{Quiz.mode=mode==='deep'?'deep':'quick';Quiz.answers={};Quiz.questionIndex=0;return Quiz.calculate()};
  Quiz.phase=score=>{const n=U.clamp(Math.round(score),0,3000);if(n<=249)return{phase:'Defining Low',step:'Absolute Low',side:'low'};if(n<=499)return{phase:'Defining Low',step:'Deep Low',side:'low'};if(n<=749)return{phase:'Defining Low',step:'Established Low',side:'low'};if(n<=999)return{phase:'Contested Low',step:'Low-Leaning',side:'low'};if(n<=1249)return{phase:'Contested Low',step:'Contested Low',side:'low'};if(n<=1499)return{phase:'Contested Low',step:'Edge Low',side:'low'};if(n===1500)return{phase:'Pivot',step:'Exact Pivot',side:'pivot'};if(n<=1749)return{phase:'Contested High',step:'Edge High',side:'high'};if(n<=1999)return{phase:'Contested High',step:'Contested High',side:'high'};if(n<=2249)return{phase:'Contested High',step:'High-Leaning',side:'high'};if(n<=2499)return{phase:'Defining High',step:'Established High',side:'high'};if(n<=2749)return{phase:'Defining High',step:'Deep High',side:'high'};return{phase:'Defining High',step:'Absolute High',side:'high'}};
  Quiz.band=(axisId,score)=>{const a=axisById[axisId];return score<1000?a.low:score<2000?a.neutral:a.high};
  Quiz.registryBand=(axisId,score)=>{const a=axisById[axisId];return score<1000?a.low:score<2000?'Neutral':a.high};
  Quiz.profile=scores=>{const key=(A.profileAxes||[]).map(id=>Quiz.registryBand(id,scores[id])).join(' — ');return(A.profiles||[]).find(p=>p.profile===key)||{name:'Unregistered Profile',profile:key,description:'The four profile-axis bands did not match a named registry entry.'}};
  function add(scores,breakdown,source,effects){for(const id of A.axisNames){const amount=U.num(effects?.[id]);if(!amount)continue;scores[id]+=amount;breakdown[id].push({source,amount})}}
  Quiz.calculate=()=>{const scores=Object.fromEntries(A.axisNames.map(x=>[x,A.midpoint||1500])),breakdown=Object.fromEntries(A.axisNames.map(x=>[x,[]]));for(const row of [...Quiz.creation.background,...Quiz.creation.covenant,...Quiz.creation.classFocus])if(row?.axis&&row.amount)add(scores,breakdown,row.source||'Creation commitment',{[row.axis]:U.clamp(U.num(row.amount),-250,250)});Quiz.questions().forEach((q,i)=>{const option=q.options?.[Quiz.answers[i]];if(option)add(scores,breakdown,`${Quiz.mode==='deep'?'Scenario':'Snapshot'} ${i+1}`,option.effects)});A.axisNames.forEach(k=>scores[k]=U.clamp(Math.round(scores[k]),A.axisMin,A.axisMax));const profile=Quiz.profile(scores),bands=Object.fromEntries(A.axisNames.map(id=>[id,Quiz.band(id,scores[id])])),phases=Object.fromEntries(A.axisNames.map(id=>[id,Quiz.phase(scores[id])])),expression=A.axisNames.filter(id=>!A.profileAxes.includes(id)).map(id=>`${axisById[id].name}: ${bands[id]} (${phases[id].step})`).join(' • ');return{scores,bands,phases,breakdown,name:profile.name,profile:profile.profile,description:profile.description,expression,answered:Object.keys(Quiz.answers).length,total:Quiz.questions().length,mode:Quiz.mode,trackers:{...Quiz.trackers}}};
  Quiz.loadFromCharacter=character=>{const saved=character?.alignment;if(saved?.mode)Quiz.mode=saved.mode;if(saved?.answers)Quiz.answers=U.clone(saved.answers);Quiz.creation=U.clone(saved?.creation||Quiz.creation);Quiz.trackers={...Quiz.trackers,...(character?.covenant||{}),...(saved?.trackers||{})};return Quiz.calculate()};
  Quiz.saveToCharacter=async character=>{const result=Quiz.calculate();character.alignment={...result,characterContext:window.ActiveWorkspaceCanon.characterSummary(character),answers:U.clone(Quiz.answers),creation:U.clone(Quiz.creation),trackers:U.clone(Quiz.trackers),visibility:character.alignment?.visibility||'private'};character.covenant={...(character.covenant||{}),resonance:Quiz.trackers.resonance,divineClaim:Quiz.trackers.divineClaim,resolve:Quiz.trackers.resolve,convictionStrain:Quiz.trackers.convictionStrain};return await window.ActiveWorkspaceCharacters.save(character)};
  window.ActiveWorkspaceAlignment=Quiz;
})();
