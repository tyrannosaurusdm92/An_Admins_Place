(function(root){'use strict';const A=root.AIBrain=root.AIBrain||{};A.searchRecords=function(query,records=[],fields=['title','summary','content','tags'],opts={}){const terms=A.tokens(query),limit=opts.limit||25;return records.map((r,i)=>{let score=0;for(const f of fields){const v=Array.isArray(r[f])?r[f].join(' '):r[f];const h=A.norm(v);for(const t of terms)if(h.includes(t))score++}return{record:r,score,index:i}}).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).slice(0,limit);};})(typeof globalThis!=='undefined'?globalThis:this);
