(function(root){function flatten(indexShards){return(indexShards||[]).flatMap(x=>x.entries||x||[]);}function byDomain(entries,domains){const d=new Set(domains||[]);return(entries||[]).filter(e=>d.has(e.domain)||(e.relatedDomains||[]).some(x=>d.has(x)));}function search(entries,q,{limit=50}={}){const terms=String(q||'').toLowerCase().match(/[a-z0-9]+/g)||[];return(entries||[]).map(e=>{const hay=[e.path,e.title,e.domain,...(e.tags||[]),...(e.capabilities||[])].join(' ').toLowerCase();let s=0;for(const t of terms)if(hay.includes(t))s++;return{entry:e,score:s};}).filter(x=>x.score).sort((a,b)=>b.score-a.score).slice(0,limit);}const api={flatten,byDomain,search};root.AIBrainCorpusIndex=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;}(typeof globalThis!=='undefined'?globalThis:this));
