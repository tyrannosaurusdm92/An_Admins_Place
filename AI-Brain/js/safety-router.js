(function(root){'use strict';
const HIGH_IMPACT=[['health-boundaries',/medical diagnosis|diagnose me|prescribe|change (?:my )?medication|psychiatr|healthcare|mental health/i],['emergency-risk',/suicid|kill myself|overdose|cannot breathe|can't breathe|severe chest|stroke|unconscious|immediate danger/i],['privacy-scope',/private|personal health|journal|secret|password|token|api key/i],['destructive-change',/delete|overwrite|merge all|replace canon|purge/i],['moderation-impact',/ban user|suspend user|moderation enforcement/i]];
function assess(input={},ctx={}){if(typeof input==='string')input={request:input,...ctx};const request=String(input.request||''),domains=new Set(input.domains||[]),flags=[];for(const [flag,rx] of HIGH_IMPACT)if(rx.test(request))flags.push(flag);if([...domains].some(d=>['healthcare','mental-health','psychiatry','physical-health'].includes(d))&&!flags.includes('health-boundaries'))flags.push('health-boundaries');if(input.minor)flags.push('minor-context');if(input.privateData&&!flags.includes('privacy-scope'))flags.push('privacy-scope');return{flags:[...new Set(flags)],humanReview:flags.some(x=>['health-boundaries','emergency-risk','moderation-impact'].includes(x)),humanAuthority:flags.some(x=>['health-boundaries','emergency-risk','moderation-impact'].includes(x)),rules:['preserve provenance','respect consent and permissions','use least-necessary context','do not silently overwrite canon/private state','qualified human or emergency care takes priority when appropriate']};}
const API={assess,HIGH_IMPACT};root.AIBrainSafetyRouter=API;if(typeof module!=='undefined'&&module.exports)module.exports=API;
})(typeof globalThis!=='undefined'?globalThis:this);
