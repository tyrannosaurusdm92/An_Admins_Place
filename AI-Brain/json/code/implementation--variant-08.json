{
  "schema": "ai-brain-implementation-pattern/1.0",
  "domain": "code",
  "capabilities": [
    "character",
    "class",
    "merge"
  ],
  "provenance": {
    "sourceGroup": "character-voice-studio",
    "sourceType": "implementation-pattern-extraction"
  },
  "sourceLanguage": "py",
  "summary": {
    "functions": [
      "_load_tts_backend_module",
      "_make_minimal_subclass",
      "generate",
      "is_available",
      "sample_rate",
      "supported_languages",
      "test_all_subclasses_have_callable_unload",
      "test_default_unload_is_idempotent",
      "test_default_unload_returns_none",
      "test_unload_defined_on_base_class",
      "test_unload_is_not_abstract",
      "test_unload_signature_takes_self_only"
    ],
    "classes": [
      "TestExistingSubclassesInherit",
      "TestUnloadDefaultBehavior",
      "TestUnloadOnABC",
      "_MinimalBackend"
    ],
    "imports": [],
    "lineCount": 157,
    "characterCount": 6188
  },
  "selectedComments": [
    "If unload() becomes @abstractmethod in this commit, every",
    "subclass that hasn't migrated yet stops instantiating. Phase 2",
    "may flip this — but Wave 1 must not.",
    "Single positional `self` parameter — engine-switch in the",
    "registry must call it with no arguments.",
    "The module uses `from __future__ import annotations`, which",
    "stringifies return annotations. Accept the string form too.",
    "Two back-to-back calls must not raise. Real overriders need this",
    "property to handle \"user spam-clicks the engine switch\" gracefully.",
    "Sanity: the file is supposed to ship at least 9 engines today."
  ]
}